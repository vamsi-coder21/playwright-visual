{
  "devDependencies": {
    "@types/node": "^25.0.3",
    "allure-playwright": "^3.9.0"
  },
  "scripts": {
    "test:visual:baseline": "playwright test tests --project=chromium --update-snapshots",
    "test:visual": "playwright test tests --project=chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.48.0",
    "playwright": "^1.48.1"
  }
}
